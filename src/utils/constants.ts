export enum Keys {
  jokes = 'jokes',
}

export enum Modes {
  edit = 'edit',
  view = 'view',
}

export enum Flags {
  NSFW = 'NSFW',
  Religious = 'Religious',
  Political = 'Political',
  Racist = 'Racist',
}

export enum Limits {
  pageLimit = 4,
  itemLimit = 4,
}
